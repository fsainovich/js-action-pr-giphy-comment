name: Testing Self-Hosted Runner
on:
  workflow_dispatch:

jobs:
  testing:
    runs-on: [self-hosted, Linux, X64]
    steps:
      - name: Echo content
        run: |
          echo -e "OK"
          sleep 1500
